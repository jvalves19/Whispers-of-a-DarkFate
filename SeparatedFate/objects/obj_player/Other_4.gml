/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

if(room != rm_0init ){
	if(room != rm_menu ){
		if(room != rm_cutsc ){
			if(room != rm_cutsc01 ){
				if(room != rm_cutscEnd1 ){
					var cam = instance_create_layer(x, y, layer, obj_camera);
					cam.target = id;
				}
			}
		}
	}
}